// Shared domain types for CRWD.
// Populated as each domain (leads, campaigns, AI, etc.) is implemented.
export type OrganizationRole = "owner" | "admin" | "manager" | "member" | "viewer";

export type RiskLevel = "low" | "medium" | "high" | "critical";
